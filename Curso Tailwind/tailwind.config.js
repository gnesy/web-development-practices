module.exports = {
    content:["./*.html"],
    theme: {
        extend: {
            fontFamily: {
        // Usa el nombre que quieras para referirte a la fuente (ej. 'roboto')
                rowdies: ['Rowdies', 'serif'],
            },
        },
    },
    plugins: [],
}